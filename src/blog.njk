---
layout: default
pagination:
    data: collections.posts
    size: 10
    alias: posts
---

{% for post in collections.posts %}
    <a class="" href="{{ post.url | url }}">{{ post.data.title }}</a>
    {{ post.date | readableDate }}</p>
{% endfor %}
