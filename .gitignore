# OS generated files
._*
.cache
.DS_Store
.Spotlight-V100
.Trashes
.vscode
ehthumbs.db
Thumbs.db

# Files and folders to ignore
dist/
node_modules/
package-lock.json

# Impeccable Live (ephemeral session data)
.impeccable/live/

# Local sizzle-reel checkout for prototype rebuilds (not a submodule)
.sizzle-reel/

# Secrets / local env (future-proof)
.env
.env.*
!.env.example
